<h1> Artist Search </h1>

{{#each artistList}}
  <div class="artist">

    <img src="{{this.thumb}}" alt="{{this.title}}" />
    <a href="/album/" class="albumBtn">{{this.title}}</a>
  </div>
{{/each}}